package com.eid.onstand.core.ui.theme

import androidx.compose.ui.graphics.Color

object Colors {
    
    // Basic Colors
    val White = Color.White
    val Black = Color.Black
    val Transparent = Color.Transparent
    val Red = Color.Red
    val Green = Color.Green
    val Blue = Color.Blue
    val Yellow = Color.Yellow
    val Magenta = Color.Magenta
    val Cyan = Color.Cyan
    
    // Primary Colors
    val Primary = Color(0xFF7B68EE)
    val PrimaryVariant = Color(0xFF5856D6)
    
    // Background Colors
    val BackgroundPrimary = Color(0xFF2C2C2C)
    val BackgroundSecondary = Color(0xFF1A1A1A)
    val BackgroundCard = Color(0xFF2A2A2A)
    val BackgroundCardTransparent = Color(0xFF1A1A1A)
    
    // Accent Colors
    val AccentOrange = Color(0xFFFF9500)
    val AccentBlue = Color(0xFF007AFF)
    val AccentTeal = Color(0xFF00C7BE)
    val AccentLightBlue = Color(0xFF64D2FF)
    val AccentGreen = Color(0xFF32D74B)
    val AccentPurple = Color(0xFFBF5AF2)
    
    // Selection and Border Colors
    val SelectionBorder = Color(0xFF7B68EE)
    val SelectionIndicator = Color(0xFF7B68EE)
    
    // Text Colors
    val TextPrimary = Color.White
    val TextSecondary = Color.White.copy(alpha = 0.7f)
    val TextDisabled = Color.White.copy(alpha = 0.5f)
    
    // Clock Text Colors - Sophisticated and elegant
    val ClockWhite = Color(0xFFF8F9FA)           // Soft white
    val ClockWarm = Color(0xFFF5E6D3)            // Warm cream
    val ClockBlush = Color(0xFFE8D5C7)           // Subtle blush
    val ClockSage = Color(0xFFB8C5B8)            // Muted sage green  
    val ClockLavender = Color(0xFFD4C5E8)        // Soft lavender
    val ClockDusk = Color(0xFFC7B8D4)            // Twilight purple
    val ClockAmber = Color(0xFFE8C07D)           // Warm amber
    val ClockMist = Color(0xFFB8D4E8)            // Soft blue-gray
    val ClockRose = Color(0xFFE8B8C7)            // Muted rose
    
    // Button Colors
    val ButtonBackground = Color.Black.copy(alpha = 0.3f)
    val ButtonPrimary = Color(0xFF7B68EE)
    val ButtonPrimaryTransparent = Color(0xFF7B68EE).copy(alpha = 0.8f)
    
    // Overlay Colors
    val OverlayBlack = Color.Black.copy(alpha = 0.6f)
    val OverlayTransparent = Color.Transparent
    
    // Clock Colors
    val ClockDefault = Color.White
    val ClockNumbers = Color.White.copy(alpha = 0.8f)
    val ClockInactive = Color.White.copy(alpha = 0.011f)
    
    // Morph Card Colors
    val MorphCardDefault = Color(0xFFFFA77A).copy(alpha = 0.85f)
    
    // Solid Background Colors
    val SolidBlack = Color(0xFF000000)
    val SolidWhite = Color(0xFFFFFFFF)
    val SolidRed = Color(0xFFFF0000)
    val SolidGreen = Color(0xFF00FF00)
    val SolidBlue = Color(0xFF0000FF)
    val SolidYellow = Color(0xFFFFFF00)
    val SolidMagenta = Color(0xFFFF00FF)
    val SolidCyan = Color(0xFF00FFFF)
    val SolidOrange = Color(0xFFFF8C00)
    val SolidPurple = Color(0xFF8A2BE2)
}